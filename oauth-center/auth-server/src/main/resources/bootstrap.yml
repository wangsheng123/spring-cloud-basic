#端口
server:
  port: 8000
#  port: ${randomServerPort.value[8000,8000]}    #随机端口

#服务名称
spring:
  application:
    name: auth-server

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always   

